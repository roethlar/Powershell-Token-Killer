# s3-background-bash-parity: declined at Slice 3 intake

`background=true` does not yet share foreground Bash delegation, but the
approved implementation sequence assigns foreground/background routing parity
to Slice 5. The candidate is therefore out of scope for the Slice 3 integrated
review, not a Slice 3 defect; retain it as a later-plan obligation.
