# sd2-6: No guard proves detection precedes forced-rtk routing

**Severity**: LOW — coverage gap on the plan's ordering requirement;
production is currently correct.
**Status**: Open
**Branch**: master (direct, per recorded precedent)
**Commit**: (filled in after commit)

## Evidence
`server/PtkMcpServer.Tests/ShellDialectWiringTests.cs` exercises detection
with rtk ABSENT (Detection_fires_with_rtk_absent) but never with rtk
present and forced. `export X=1` satisfies the resolver's
single-command/constant-args shape, and route=rtk skips the Application
check — so post-routing detection would see `& '<rtk>' export X=1`
(operator-invoked, `export` in argument position): no finding.

## Predicted observable failure
An implementation that routes before detecting would execute the rtk
rewrite instead of refusing under route="rtk" — with every current
foreground wiring test green.

## What
The "detection runs before routing" ordering (plan slice 1(i) rationale,
wired in slice 2) has no guard on the rtk-present leg.

## Approach
Add a forced-rtk test: executable rtk stub via PTK_RTK_PATH, route:"rtk",
`export X=1` → assert the dialect refusal and no stub output. Red leg:
temporarily reorder detection after ResolveScript — the new test must
fail; restore — pass.

## Files changed
(filled in after commit)

## Guard proof
(filled in after commit)

## Coder dispute (if any)
None — admitted; confirmed by inspection of the test file and resolver.

## Known gaps
None.

## Reviewer comments
codex (codex-cli 0.144.0, read-only), reviewed 8c234e8 (base 3121fc0),
2026-07-09/10 UTC: initial-pass finding (LOW), evidence and predicted
failure as above.
