# sd4-1: Server README overstates detector coverage

**Severity**: LOW — misleading documentation only; the bounded-recall
implementation is intentional and correct.
**Status**: In progress
**Branch**: master (direct, per recorded precedent)
**Commit**: (filled in after commit)

## Evidence
`server/README.md` routing bullet (added in slice 4): "Probed bash-only
constructs get a fast labeled `[ptk:dialect]` refusal" — reads as
whole-class coverage. Trailing-`\` continuation is a probed, frozen
ACCEPTED MISS (plan slice 0; a Pester test pins it), so a reader-predicted
refusal does not happen for it.

## Predicted observable failure
A user or agent reading server/README expects a refusal for a bash-only
shape that is off the detection list; it instead runs with changed
semantics (foreground) or starts a job (background). Docs-vs-behavior
mismatch only.

## What
The bullet omits the plan's precision-over-recall bound that the main
README paragraph states.

## Approach
Reword to "constructs on the detector's probed detection list" and add the
undetected-shapes-run-as-before sentence, matching README.md.

## Files changed
- `server/README.md` — routing bullet reworded with the recall bound.

## Guard proof
Docs-only wording; no automated guard exists for README prose (consistent
with prior docs-only findings). Verified by re-reading both READMEs for
consistency.

## Coder dispute (if any)
None — admitted.

## Known gaps
None.

## Reviewer comments
codex (codex-cli 0.144.0, read-only), reviewed 8bb96b1 (base f0f3238),
2026-07-10 UTC: initial-pass finding (LOW).
