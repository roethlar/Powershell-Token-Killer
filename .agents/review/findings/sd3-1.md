# sd3-1: Several raw surfaces omit D2's required fidelity pairing

**Severity**: MEDIUM (as filed) — admitted IN PART; the admitted part is
LOW-to-MEDIUM (an incompletely-stated pairing on the raw parameter), the
declined part would have been misadvice.
**Status**: Verified (admitted part fixed; declined part closed by
owner-delegated adjudication 2026-07-10)
**Branch**: master (direct, per recorded precedent)
**Commit**: `1c92cd6` (admitted part)

## Evidence (as filed by the reviewer)
`server/PtkMcpServer/Tools/InvokeTool.cs` raw parameter description,
`README.md:43-44`, `server/README.md:86-87`, and the three elision markers
in `src/PwshTokenCompressor.psm1` describe raw without explicitly teaching
`route=pwsh` + `raw=false`; the current assertions guard the incomplete
wording, so the battery stays green.

## Predicted observable failure (as filed)
A model following one of these surfaces reruns with raw=true instead of
selecting route=pwsh + raw=false, producing unshaped output and
unnecessary context expansion, violating D2(b).

## Triage: ADMITTED IN PART
- **ADMITTED — raw parameter description.** It redirects the fidelity
  habit ("use route=pwsh instead") but does not name `raw=false`, the
  pairing's second half as the plan states it verbatim. Fixed, with the
  assertion strengthened to require the full pairing phrase.
- **DECLINED — the elision markers and the two README sentences that
  describe them.** Coder-verified before triage (live, 2026-07-10):
  elision applies to ALL shaped output regardless of route —
  `route=pwsh` + `raw=false` returns the SAME elided window (`line 500`
  absent from shaped output of a 1000-line probe). On those surfaces the
  motive is the elided middle, and raw=true is the only recovery;
  teaching the pairing there would be false advice, the exact class of
  wrong-guidance defect sd2-1 just removed. D2(b)'s rationale (issue #4's
  agent used raw out of FIDELITY habit) does not apply to the
  elided-middle motive. Both READMEs teach the pairing explicitly in
  their adjacent overrides paragraphs, so no louder-surface inversion
  remains.

## Approach
`InvokeTool.cs` raw parameter description now ends "use route=pwsh with
raw=false instead"; `RawUsageTests` requires the full pairing phrase on
the parameter description.

## Files changed
- `server/PtkMcpServer/Tools/InvokeTool.cs` — raw parameter description
  names the full pairing.
- `server/PtkMcpServer.Tests/RawUsageTests.cs` — the parameter assertion
  requires "route=pwsh with raw=false".

## Guard proof
- `RawUsageTests::Invoke_descriptions_teach_recovery_only_raw_and_the_pwsh_pairing`
  — red leg run 2026-07-10: with the InvokeTool fix stashed, the
  strengthened assertion FAILS on the old parameter text; restored,
  PASSES.

## Coder dispute (if any)
The partial decline above, with the live elision-vs-route proof. If the
reviewer holds the marker surfaces NOT RESOLVED on re-grade, the split
routes to the owner as contested rather than being implemented as
misadvice.

## Known gaps
None beyond the recorded decline.

## Reviewer comments
codex (codex-cli 0.144.0, read-only), reviewed fa1b23c (base d72adcb),
2026-07-10 UTC: initial-pass finding (MEDIUM), evidence and predicted
failure as filed above.

Re-grade round 1: codex (codex-cli 0.144.0, read-only), reviewed 1c92cd6
(base fa1b23c), 2026-07-10 UTC — **CONTESTED**: admitted raw-parameter
scope "correctly fixed and guarded"; the elision premise granted as true,
but the reviewer held that approved D2 requires the pairing per-surface
and that "omitting those surfaces requires owner amendment".

**Adjudication (2026-07-10, in-session):** routed to the owner per the
playbook; the owner delegated the call to the implementing agent under
the agent-experience principle now recorded in
`.agents/repo-guidance.md` (Earned Practices). Call: the decline stands —
markers stay lean; the D2 amendment scoping the pairing requirement to
fidelity-motive surfaces is recorded in `.agents/decisions.md`. Finding
CLOSED (the exact resolution codex named: owner amendment).
