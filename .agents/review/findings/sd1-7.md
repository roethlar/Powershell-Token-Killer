# sd1-7: parse-fatal error IDs and shape evidence cross-associate globally

**Severity**: LOW — the script is unparsable either way; the harm is dialect
guidance wrongly naming bash (sd1-3 harm class).
**Status**: RESOLVED — loop closed CONVERGED after the round-4 fix (`0c43b05`); see Reviewer comments
**Commits**: `ef9f3ed` (r2 overlap-or-after locality) + `f30ddde` (r3 keyword evidence must sit in command position; guard: 'ignores a keyword in argument position even after the error (sd1-7 round 3)')
**Branch**: master (direct), per loop precedent

## Evidence
Reported by codex at re-grade round 1 (head `acb0f39`): the parse-fatal
branch pairs the SET of all parser error IDs with regexes over ALL of the
scan text (psm1:610) — the error and the shape evidence need not come from
the same statement.
**Master verified live at head:** `Write-Output then; if $true` — the only
error is `MissingOpenParenthesisInIfStatement` on the malformed pwsh `if`,
while `then` is an ordinary earlier argument; the detector returns
`a bash if/then/fi statement`.

## Predicted observable failure
Once wired, a user with a malformed PowerShell `if` whose script happens to
contain a bash keyword elsewhere gets bash-dialect guidance instead of the
real pwsh syntax error — wrong guidance.

## Approach (proposed, fix round 2)
Localize the association: require the shape-regex match and the paired error
extent to overlap or be adjacent (same statement/line window), rather than
pairing any error ID with any match anywhere in the script.
Guard: the exact repro expects null; existing parse-fatal IN tests keep recall.

## Reviewer comments
Origin: codex re-grade round 1 new-finding #4 (Codex v0.144.0, gpt-5.6-sol).

Re-grade round 3 (codex, gpt-5.6-sol, read-only, head `374666b`):
**NOT RESOLVED** — a script-defined function literally named `then`,
invoked after an unrelated malformed `if`, still read as bash evidence
(master-verified; the submitted script itself remains unparsable — LOW
class). Round 4 (`0c43b05`): keyword commands that are locally defined
before their use, or that resolve in the session, are not bash evidence.
Loop closed CONVERGED at this point per the recorded contrived-tail
precedent. Owner may reopen.

Re-grade round 2 (codex, gpt-5.6-sol, read-only, head `293eda6`):
**NOT RESOLVED** — the at/after prong accepted any match after the error's
start with no boundary: `if $true; Write-Output then` still returned the
if/then finding from an argument-position keyword. **Master verified the
repro at head before round 3.** Round 3 (`f30ddde`): probing showed pwsh
error recovery leaves genuine bash `then`/`done` in COMMAND position
(`then@17`, `fi@31`, `done@28`) while argument keywords never are, so the
if/then and do/done keys now require a bareword CommandAst name, still
paired with the error id and the locality predicate.
