# sd1-1: clean-parse classifiers lack the shadowing guard the router already has

**Severity**: MEDIUM — once wired (slice 2), a session-defined `function export`/`local`/`source` is refused even though it resolves and runs correctly in the warm runspace.
**Status**: RESOLVED — loop closed CONVERGED after the round-4 fix (`eb5e193`); see Reviewer comments
**Branch**: master (direct)
**Commit**: `ca0a7e2` (r1 ambient GetCommand) + `bc5638d` (r2 script-local function definitions) + `9b5e326` (r3 preceding Set-Alias/New-Alias in positional and -Name spellings, lexical ordering enforced; guards: 'honors a preceding Set-Alias definition, in both spellings (sd1-1 round 3)' and 'only definitions that lexically PRECEDE the use exempt it (sd1-1 round 3)')

## Evidence
`src/PwshTokenCompressor.psm1:643` (slice-1 head `6694dc5`): the `export`/`local`/`source`/`VAR=` classifiers key on name shape alone. `Resolve-PtcInvokeScript` (same file) already guards the equivalent decision with `$ExecutionContext.InvokeCommand.GetCommand(...)`.

## Predicted observable failure
`function export { param($Assignment) "ran:$Assignment" }; export X=1` executes successfully in the warm runspace, yet the detector returns `the bash 'export' builtin` — the refusal path would reject valid PowerShell.

## What
The CommandNotFound-class classifiers assert a failure that cannot happen when the name actually resolves in the session.

## Approach
Compute the candidate label first; before returning it, resolve the name with the same intrinsic the router uses. If it resolves to anything, the predicted failure is impossible — skip the finding. `set` is intentionally exempt: it resolves (alias) in every pwsh by design and is keyed to its argument shape instead (frozen slice-0 decision).

## Files changed
- `src/PwshTokenCompressor.psm1` — resolution guard around the CommandNotFound-class returns
- `tests/PwshTokenCompressor.Tests.ps1` — shadowed-`export` test (define `global:export`, expect null; remove, expect finding)

## Guard proof
`tests/PwshTokenCompressor.Tests.ps1::'stays silent when the bash name is shadowed by a real session command'` — FAILS with the guard reverted, PASSES with it restored.

## Coder dispute (if any)
None.

## Known gaps
The guard resolves in the detector's runspace; the cold `JobManager` child resolves nothing extra, so behavior there is unchanged. Alias definitions written with splatting or with other named parameters ahead of the positional name are not tracked (round 3): those uses stay flagged — a residual over-match on exotic-but-valid scripts, accepted as the bounded cost of not re-implementing Set-Alias binding.

## Reviewer comments
Re-grade round 1 (codex, Codex v0.144.0, gpt-5.6-sol, read-only, head `acb0f39`):
**NOT RESOLVED.** The guard resolves only AMBIENT session commands via
`GetCommand` (psm1:681); definitions inside the submitted script itself are
never seen. The finding's own predicted-failure repro —
`function export { param($Assignment) "ran:$Assignment" }; export X=1` —
still returns the bash-export finding even though it executes (`ran:X=1`).
The named guard (Tests.ps1:1233) passes but tests the ambient
`global:export` shape only, not the inline-definition failure the finding
predicted. **Master verified the repro independently at head: finding still
returned.** Fix round 2 must also honor script-local definitions of the
candidate name (function definitions / alias assignments in the submitted
text) before returning a CommandNotFound-class label.

Re-grade round 3 (codex, gpt-5.6-sol, read-only, head `374666b`):
**NOT RESOLVED** — recursion: a same-name use INSIDE its own definition
body (`function export { ... export X=1 ... }; export again`) executes
(prints ran:X=1; master-verified) yet was flagged, because the lexical rule
required the definition's END to precede the use. Round 4 (`eb5e193`): a
use inside its own definition's extent counts as defined. Loop closed
CONVERGED at this point per the recorded contrived-tail precedent — three
consecutive rounds produced strictly more crafted repros under the same ID
with no new IDs; every named repro now has a guard. Owner may reopen.

Re-grade round 2 (codex, gpt-5.6-sol, read-only, head `293eda6`):
**NOT RESOLVED** — round 2 covered function definitions only, but the plan
(slice 1(iii)) also names `Set-Alias export ...` as a preceding definition:
`Set-Alias export Write-Output; export X=1` executes (prints X=1) yet was
still flagged. **Master verified the repro at head before round 3.** Round 3
(`9b5e326`) adds Set-Alias/New-Alias tracking (positional + -Name spellings)
and enforces the plan's lexical-precede rule for functions and aliases both;
exotic alias spellings (splatting, leading other named parameters) remain
accepted residuals — recorded under Known gaps.
